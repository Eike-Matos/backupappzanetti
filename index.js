/**
 * @format
 */

import {AppRegistry} from 'react-native';
import OrdensServico from './src/screens/OrdensServico';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => OrdensServico);
